{
  "contract_id": "entry-points-v1",
  "purpose": "Declare allowed runtime entry points and mandatory execution metadata.",
  "design_intent": "Prevent implicit flows by forcing explicit trigger/mode/precondition declarations.",
  "change_impact": "Entry-point changes affect API invocations, scheduler ticks, and audit traceability.",
  "required_entry_fields": [
    "name",
    "trigger",
    "mode",
    "start_step",
    "preconditions",
    "end_conditions",
    "idempotency"
  ],
  "allowed_modes": ["auto", "manual_override", "dry_run", "bootstrap"],
  "required_preconditions": [
    "env_valid",
    "run_lock_available"
  ],
  "required_runtime_metadata": [
    "run_id",
    "entry_point",
    "mode",
    "started_at_utc",
    "outcome"
  ],
  "failure_policies": [
    "abort_startup",
    "fail_closed",
    "manual_review",
    "retry_once_then_error"
  ]
}
